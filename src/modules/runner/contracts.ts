export type RunnerCapability = {
  namespace: "/api/runner";
  enabled: false;
  strategy: "reserved";
};

export const runnerCapability: RunnerCapability = {
  namespace: "/api/runner",
  enabled: false,
  strategy: "reserved",
};
